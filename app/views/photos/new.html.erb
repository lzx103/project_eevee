<h1>Upload Photo</h1>
<%= form_for @photo, :html => {:multipart => true} do |f| %>
	<%= f.label "Choose image to upload: " %>
	<%= f.file_field :image%>
	<%= f.label :description %>
	<%= f.text_field :description%>
	<%= f.submit "upload" %>
<%end%>